<%= render 'shared/errors', obj: @cheff %>

<div class="row">
	<div class="well col-md-8 col-md-offset-2">
		<%= form_for @cheff do |f| %>   
		
		    <%= f.label :chefname %>
		    <%= f.text_field :chefname %>
        
        <%= f.label :email %>
        <%= f.email_field :email %> 

        <%= f.label :password %>
        <%= f.password_field :password %>


        <%= f.submit(@cheff.new_record? ? 'Register' : 'Edit Profile', class: "btn btn-success") %>
     <% end %>    
	</div>
</div>

